# Progress — Digital Service Note System

## Current Status
- Current task: TASK 13 — PDF Template and PDF Generation
- Completed tasks: TASK 00, TASK 01, TASK 02, TASK 03, TASK 04, TASK 05, TASK 06, TASK 07, TASK 08, TASK 09, TASK 10, TASK 11, TASK 12, TASK 13
- Next task: TASK 14 — Print and Download PDF Buttons
- Last successful command: `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Known errors:
  - `git status --short` failed because this folder is not a Git repository.
  - `pdftotext` is not installed.
  - PDF text extraction with `pypdf` returned empty text because the PDF appears image-based/scanned.
  - `docker run --rm -v "${PWD}:/app" -w /app composer:latest create-project laravel/laravel laravel-temp` failed because Docker Desktop Linux engine / Docker daemon is not running.
  - `docker compose up -d --build` failed for the same Docker daemon issue.
  - TASK 02 `docker compose up -d --build` retry failed because the Docker daemon is still unavailable.
  - TASK 03 PHP syntax checks and migrations could not be run because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 04 seed execution could not be run because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 05 PHP route rendering could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 06 PHP route rendering/browser testing could not be run because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 07 runtime database save could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 08 runtime service number generation/database save could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 09 runtime homepage search/database query behavior could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 10 runtime service note detail page rendering could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 11 runtime edit/update database behavior could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 12 runtime delete/soft-delete database behavior could not be tested because PHP is not installed locally and Docker daemon is unavailable.
  - TASK 13 Composer package install and runtime PDF generation could not be tested because Composer/PHP are not installed locally and Docker daemon is unavailable.
- Known limitations:
  - Docker files are present and `docker compose config` validates, but containers were not started because Docker daemon is unavailable.
  - PHP and Composer are not installed locally; Laravel dependencies will be installed inside the app container when Docker is running.
  - PDF reference was inspected visually from extracted embedded image, not via selectable text.

## Completed Work

### TASK 00 — Project Inspection / Setup Planning
- Date/time: 2026-05-10 14:01:14 +08:00
- Summary:
  - Read `PRD.md`.
  - Read `Pembahagian Tugasan.md`.
  - Inspected project folder.
  - Confirmed this folder currently contains documentation only.
  - Confirmed there is no existing Laravel codebase.
  - Confirmed there is no existing Docker setup.
  - Confirmed there is no Git repository initialized in this folder.
  - Checked the reference PDF at `c:\Users\Jokeryazid\Downloads\servicenote_one_service.pdf`.
  - Extracted the embedded image from the PDF into a temporary local BMP for visual inspection.
  - Identified the PDF form structure for future UI/PDF work.
- PDF reference notes:
  - Company/header area: LaptopPro, SERVICE REPORT, address, mobile phone, office phone, support email.
  - Service number field at top right.
  - Top form uses two-column layout:
    - Maklumat Pelanggan / Customer Information: name, phone, received date.
    - Maklumat Peranti / Device Information: type checkbox, model, serial number, specification, password.
  - Main body uses horizontal service sections:
    - Masalah Dilaporkan / Customer Reported Issue.
    - Pemeriksaan Awal / Initial Diagnosis.
    - Kerja Baik Pulih / Repair Action Taken.
  - Bottom area:
    - Alat Ganti / Parts Replaced.
    - Kos Baiki / Charges: Upah Servis, Alat Ganti, Jumlah.
    - Waranti Servis: duration in days and warranty exclusion note.
    - Customer and technician signature lines.
- Files created:
  - `TASKS.md`
  - `PROGRESS.md`
- Files changed:
  - None.
- Commands run:
  - `Get-ChildItem -Force`
  - `Get-Content -Raw -LiteralPath .\PRD.md`
  - `Get-Content -Raw -LiteralPath '.\Pembahagian Tugasan.md'`
  - `Get-Item -LiteralPath 'c:\Users\Jokeryazid\Downloads\servicenote_one_service.pdf' | Format-List FullName,Length,LastWriteTime`
  - `rg --files`
  - `git status --short`
  - `pdftotext -layout 'c:\Users\Jokeryazid\Downloads\servicenote_one_service.pdf' -`
  - Python checks for installed PDF/image libraries.
  - Python `pypdf` inspection of page count, text, and image XObject metadata.
  - Python extraction of PDF embedded image to temporary PPM/BMP for visual inspection.
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - TASK 00 deliverables completed.
  - No application tests run because no application exists yet.
- Notes:
  - The next implementation step should create the Laravel project and Docker Compose base.
  - Keep homepage focused on the service note form, with search on the same page.
  - Later UI and PDF tasks should use the visual reference PDF layout closely.

### TASK 01 — Laravel + Docker Compose Base Setup
- Date/time: 2026-05-10 14:07:54 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current codebase and confirmed TASK 01 was the next incomplete task.
  - Checked local tooling.
  - Found Docker CLI and Docker Compose are installed.
  - Found PHP and Composer are not installed locally.
  - Attempted to create Laravel through the Composer Docker image, but Docker daemon was not running.
  - Downloaded the official Laravel 13.x application skeleton from GitHub and moved it into the project root.
  - Added Docker Compose setup with an app service and MariaDB database service.
  - Added persistent Docker volumes for MariaDB data and Composer vendor dependencies.
  - Added a Docker entrypoint that installs Composer dependencies, creates `.env` from `.env.example` when missing, generates `APP_KEY`, and starts Laravel on port 8000.
- Files created:
  - Laravel application directories: `app/`, `bootstrap/`, `config/`, `database/`, `public/`, `resources/`, `routes/`, `storage/`, `tests/`
  - Laravel base files: `.editorconfig`, `.env.example`, `.gitattributes`, `.gitignore`, `.npmrc`, `artisan`, `composer.json`, `package.json`, `phpunit.xml`, `README.md`, `vite.config.js`
  - `Dockerfile`
  - `docker-compose.yml`
  - `.dockerignore`
  - `docker/entrypoint.sh`
- Files changed:
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files`
  - `php -v`
  - `composer --version`
  - `docker --version; docker compose version`
  - `node --version; npm --version`
  - `docker run --rm -v "${PWD}:/app" -w /app composer:latest create-project laravel/laravel laravel-temp`
  - `Invoke-WebRequest -Uri 'https://github.com/laravel/laravel/archive/refs/heads/13.x.zip' -OutFile ...`
  - `Expand-Archive ...`
  - PowerShell `Move-Item` commands to move Laravel skeleton files into the project root.
  - `Get-Content -Raw -LiteralPath .\composer.json`
  - `Get-Content -Raw -LiteralPath .\package.json`
  - `Get-Content -Raw -LiteralPath .\.env.example`
  - `docker compose config`
  - `Get-ChildItem -Force | Select-Object Name,Mode,Length`
  - `rg --files | Sort-Object | Select-Object -First 80`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `docker compose config` passed.
  - `docker compose up -d --build` could not run because Docker Desktop Linux engine / Docker daemon is not active.
  - Application runtime was not browser-tested because containers could not start in the current environment.
- Notes:
  - TASK 01 deliverables are in place.
  - Start Docker Desktop or the Docker daemon before running `docker compose up -d --build`.
  - Next task should configure environment, database connection details, and initial README setup.

### TASK 02 — Environment, Database Connection, and README Setup
- Date/time: 2026-05-10 14:10:58 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current Laravel/Docker codebase.
  - Confirmed TASK 02 was the next incomplete task.
  - Updated `.env.example` from Laravel defaults to Digital Service Note System defaults.
  - Configured `.env.example` for the Docker MariaDB service:
    - `DB_CONNECTION=mariadb`
    - `DB_HOST=database`
    - `DB_PORT=3306`
    - `DB_DATABASE=digital_service_note`
    - `DB_USERNAME=service_note`
    - `DB_PASSWORD=service_note_password`
  - Updated Docker Compose app environment to match the application name, app URL, and MariaDB connection.
  - Replaced the default Laravel README with an initial project README containing requirements, Docker commands, environment notes, database connection check command, and access URL.
- Files created:
  - None.
- Files changed:
  - `.env.example`
  - `docker-compose.yml`
  - `README.md`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\.env.example`
  - `Get-Content -Raw -LiteralPath .\docker-compose.yml`
  - `Get-Content -Raw -LiteralPath .\README.md`
  - `Get-Content -Raw -LiteralPath .\config\database.php`
  - `docker compose config --quiet`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `docker compose config --quiet` passed.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Actual Laravel-to-MariaDB connection could not be confirmed in this environment.
  - README now includes the command to confirm database connection once Docker is running: `docker compose exec app php artisan migrate:status`.
- Notes:
  - Start Docker Desktop or the Docker daemon before runtime verification.
  - Next task should create application migrations and models.

### TASK 03 — Database Migrations and Models
- Date/time: 2026-05-10 14:14:51 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current Laravel migrations, models, seeders, routes, and config files.
  - Confirmed TASK 03 was the next incomplete task.
  - Removed Laravel default user artifacts because the MVP has no login/user management:
    - `app/Models/User.php`
    - `database/factories/UserFactory.php`
    - `database/migrations/0001_01_01_000000_create_users_table.php`
  - Replaced the default user/session migration with a sessions-only migration for database-backed Laravel sessions.
  - Created migrations for:
    - `customers`
    - `devices`
    - `service_notes`
    - `service_note_logs`
    - `settings`
  - Added indexes required by the PRD for common lookup/search fields.
  - Added `SoftDeletes` support to `service_notes`.
  - Added Eloquent models and relationships:
    - Customer has many devices.
    - Customer has many service notes.
    - Device belongs to customer.
    - Device has many service notes.
    - ServiceNote belongs to customer.
    - ServiceNote belongs to device.
    - ServiceNote has many logs.
    - ServiceNoteLog belongs to service note.
  - Hid `device_password` by default on the `Device` model.
  - Neutralized `config/auth.php` user provider/password reset references so the codebase does not reference a missing User model.
  - Cleared the default `DatabaseSeeder` user creation; sample data will be added in TASK 04.
- Files created:
  - `app/Models/Customer.php`
  - `app/Models/Device.php`
  - `app/Models/ServiceNote.php`
  - `app/Models/ServiceNoteLog.php`
  - `app/Models/Setting.php`
  - `database/migrations/0001_01_01_000000_create_sessions_table.php`
  - `database/migrations/2026_05_10_000001_create_customers_table.php`
  - `database/migrations/2026_05_10_000002_create_devices_table.php`
  - `database/migrations/2026_05_10_000003_create_service_notes_table.php`
  - `database/migrations/2026_05_10_000004_create_service_note_logs_table.php`
  - `database/migrations/2026_05_10_000005_create_settings_table.php`
- Files changed:
  - `config/auth.php`
  - `database/seeders/DatabaseSeeder.php`
  - `TASKS.md`
  - `PROGRESS.md`
- Files deleted:
  - `app/Models/User.php`
  - `database/factories/UserFactory.php`
  - `database/migrations/0001_01_01_000000_create_users_table.php`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files app database config routes | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\app\Models\User.php`
  - `Get-Content -Raw -LiteralPath .\database\migrations\0001_01_01_000000_create_users_table.php`
  - `Get-Content -Raw -LiteralPath .\database\seeders\DatabaseSeeder.php`
  - `Get-Content -Raw -LiteralPath .\database\factories\UserFactory.php`
  - `rg "User|users|password_reset" app database config routes`
  - `Get-ChildItem -LiteralPath .\database\migrations | Sort-Object Name | Select-Object Name,Length`
  - `Get-ChildItem -LiteralPath .\app\Models | Sort-Object Name | Select-Object Name,Length`
  - `docker compose config --quiet`
  - `Get-Content -Raw -LiteralPath .\config\auth.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNote.php`
  - `rg "App\\Models\\User|User::class|password_reset_tokens|Schema::create\('users'|users' =>|UserFactory" app database config routes`
  - `php -v`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - Confirmed created migration and model files are present.
  - Confirmed no remaining user model/table/password reset references in `app`, `database`, `config`, or `routes`.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `php artisan migrate` was not run because PHP is unavailable locally and Docker daemon is not running.
- Notes:
  - Once Docker is running, validate this task with `docker compose exec app php artisan migrate:fresh`.
  - Next task should add seeders for default settings and sample customer/device/service note data.

### TASK 04 — Seeders for Default Settings and Sample Data
- Date/time: 2026-05-10 14:17:33 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current seeders and Eloquent models.
  - Confirmed TASK 04 was the next incomplete task.
  - Created `DefaultSettingsSeeder` for default company settings.
  - Created `SampleServiceNoteSeeder` for sample customer, device, service note, and service note log.
  - Updated `DatabaseSeeder` to call the new seeders.
  - Kept sample data aligned with PRD:
    - Customer: Ahmad Test, `0123456789`, `ahmad@example.com`.
    - Device: Laptop, Lenovo ThinkPad Test, `TEST12345`.
    - Service note: `SN-{current year}-0001`, Received, `Laptop tidak boleh ON`, `Perlu pemeriksaan lanjut`, zero charges.
  - Used `updateOrCreate` / `firstOrCreate` so seeders can run repeatedly without duplicating sample records.
- Files created:
  - `database/seeders/DefaultSettingsSeeder.php`
  - `database/seeders/SampleServiceNoteSeeder.php`
- Files changed:
  - `database/seeders/DatabaseSeeder.php`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files app database config routes | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\database\seeders\DatabaseSeeder.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Customer.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Device.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNote.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Setting.php`
  - `Get-ChildItem -LiteralPath .\database\seeders | Sort-Object Name | Select-Object Name,Length`
  - `Get-Content -Raw -LiteralPath .\database\seeders\DefaultSettingsSeeder.php`
  - `Get-Content -Raw -LiteralPath .\database\seeders\SampleServiceNoteSeeder.php`
  - `rg "App\\Models\\User|User::class|UserFactory|user_id|password_reset_tokens|Test User" app database config routes`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - Seeder files are present and wired into `DatabaseSeeder`.
  - Confirmed no default Laravel user seed references remain.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - `php artisan db:seed` was not run due to the same environment limitations.
- Notes:
  - Once Docker is running, validate this task with `docker compose exec app php artisan migrate:fresh --seed`.
  - Next task should create the main homepage route and layout.

### TASK 05 — Main Homepage Route and Layout
- Date/time: 2026-05-10 14:21:24 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current routes, views, CSS, JS, and Vite config.
  - Confirmed TASK 05 was the next incomplete task.
  - Replaced the default Laravel welcome page with a project homepage.
  - Created the main Blade layout with:
    - App title/header.
    - LaptopPro brand area.
    - Simple navigation for Main Form, Search Records, Settings, and Backup Guide.
    - Vite/Tailwind loading when build assets or hot reload are available.
    - Small fallback CSS for readable output before assets are built.
  - Created homepage content that directly shows:
    - Service Note Form section first.
    - Search Old Records section on the same page.
  - Kept this task scoped to layout/route only; full form fields remain for TASK 06.
  - Ran frontend dependency install and production build successfully.
- Files created:
  - `resources/views/layouts/app.blade.php`
  - `resources/views/home.blade.php`
  - `package-lock.json`
  - `public/build/manifest.json`
  - `public/build/fonts-manifest.json`
  - `public/build/assets/*`
  - `node_modules/` local dependency directory
- Files changed:
  - `routes/web.php`
  - `TASKS.md`
  - `PROGRESS.md`
- Files deleted:
  - `resources/views/welcome.blade.php`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files routes resources config app | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\welcome.blade.php`
  - `Get-Content -Raw -LiteralPath .\resources\css\app.css`
  - `Get-Content -Raw -LiteralPath .\resources\js\app.js`
  - `Get-Content -Raw -LiteralPath .\vite.config.js`
  - `npm install`
  - `npm run build`
  - `Get-ChildItem -Recurse -File -LiteralPath .\resources\views | Select-Object FullName,Length`
  - `Get-ChildItem -Recurse -File -LiteralPath .\public\build | Select-Object FullName,Length`
  - `rg -i "dashboard|login|logout|admin|role" routes resources app config database`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm install` passed with 0 vulnerabilities reported.
  - `npm run build` passed and generated Vite/Tailwind assets in `public/build`.
  - Confirmed `/` route now returns `home`.
  - Confirmed view files are present.
  - Confirmed no `dashboard`, `login`, `logout`, `admin`, or `role` matches in `routes`, `resources`, `app`, `config`, or `database`.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Browser/runtime render was not tested due to PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate this task by opening `http://localhost:8000`.
  - Next task should build the complete service note form UI.

### TASK 06 — Service Note Form UI
- Date/time: 2026-05-10 14:26:41 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current homepage, layout, CSS, JS, route, and project files.
  - Confirmed TASK 06 was the next incomplete task.
  - Replaced the homepage placeholder with a complete responsive service note form UI.
  - Added the requested Bahasa Melayu form sections:
    - Maklumat Service.
    - Maklumat Pelanggan.
    - Maklumat Peranti.
    - Masalah Dilaporkan.
    - Pemeriksaan Awal.
    - Kerja Baik Pulih.
    - Alat Ganti & Kos.
    - Warranty & Signature.
  - Added UI fields for all MVP form areas, including service number, received date, status, technician, customer, device, issue, diagnosis, repair action, parts, charges, warranty, and signature placeholders.
  - Added default received date using `now()->toDateString()`.
  - Added default warranty note: `Tidak termasuk kerosakan fizikal / liquid damage.`
  - Added device password warning text.
  - Added main action buttons: Save Service Note, Save & Generate PDF, Print PDF, Download PDF, Clear Form / New Form.
  - Added frontend JavaScript for total charge calculation and `Others` device type field handling.
  - Rebuilt frontend assets with Vite/Tailwind.
- Files created:
  - None.
- Files changed:
  - `resources/views/home.blade.php`
  - `resources/js/app.js`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files routes resources app config database | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\resources\views\home.blade.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\layouts\app.blade.php`
  - `Get-Content -Raw -LiteralPath .\resources\js\app.js`
  - `Get-Content -Raw -LiteralPath .\resources\css\app.css`
  - `npm run build`
  - `rg "Maklumat Service|Maklumat Pelanggan|Maklumat Peranti|Masalah Dilaporkan|Pemeriksaan Awal|Kerja Baik Pulih|Alat Ganti & Kos|Warranty & Signature|Data ini sensitif|data-service-charge|data-device-type" resources\views\home.blade.php resources\js\app.js`
  - `Get-ChildItem -Recurse -File -LiteralPath .\public\build | Sort-Object FullName | Select-Object FullName,Length`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed all eight TASK 06 form sections are present in `resources/views/home.blade.php`.
  - Confirmed sensitive device password warning is present.
  - Confirmed frontend total-charge and device-type helper hooks are present.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Browser/runtime render was not tested due to PHP/Docker environment limitations.
- Notes:
  - Current action buttons are UI-only; persistence and post-submit behavior belong to TASK 07.
  - Once Docker is running, validate visually at `http://localhost:8000`.
  - Next task should implement storing service notes into the database.

### TASK 07 — Store Service Note into Database
- Date/time: 2026-05-10 14:31:35 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected current form, routes, models, and service note migration.
  - Confirmed TASK 07 was the next incomplete task.
  - Created `ServiceNoteController`.
  - Added `store()` method with Laravel validation for all required MVP fields.
  - Added backend total charge calculation from `service_charge + parts_charge`.
  - Added customer reuse/update by `customer_phone`.
  - Added device reuse by matching existing `customer_id + serial_number` when serial number is provided.
  - Added service note creation inside a database transaction.
  - Added created audit log entry through the `ServiceNote` logs relationship.
  - Added POST route `/service-notes` named `service-notes.store`.
  - Updated the homepage form to POST to `service-notes.store`.
  - Added validation error display and success flash message on the homepage.
  - Updated form fields to use `old()` values after validation failure.
  - Changed Save and Save & Generate PDF buttons to submit buttons.
  - Used temporary unique service numbers (`SN-TEMP-...`) only so TASK 07 can save rows before TASK 08 implements the official `SN-YYYY-0001` generator.
- Files created:
  - `app/Http/Controllers/ServiceNoteController.php`
- Files changed:
  - `routes/web.php`
  - `resources/views/home.blade.php`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files app routes resources database config | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\resources\views\home.blade.php`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Customer.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Device.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNote.php`
  - `Get-Content -Raw -LiteralPath .\database\migrations\2026_05_10_000003_create_service_notes_table.php`
  - `npm run build`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `rg "service-notes.store|session\('success'\)|\$errors->any|type=\"submit\"|old\('customer_name'|temporaryServiceNo|DB::transaction|firstOrNew|firstOrCreate|total_charge" app routes resources`
  - `rg -F "service-notes.store" app routes resources`
  - `rg -F "session('success')" resources`
  - `rg -F '$errors->any' resources`
  - `rg -F 'type="submit"' resources`
  - `Select-String -Path .\resources\views\home.blade.php -Pattern 'type="submit"'`
  - `rg -F "DB::transaction" app`
  - `rg -F "temporaryServiceNo" app`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed POST route `service-notes.store` exists and the form points to it.
  - Confirmed success flash and validation error display are present.
  - Confirmed Save buttons are submit buttons.
  - Confirmed controller uses `DB::transaction`.
  - Confirmed controller contains temporary service number creation for TASK 07.
  - `docker compose config --quiet` passed.
  - One `rg` command failed due to a regex escaping mistake; follow-up literal searches passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Runtime form submission/database save was not tested due to PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate with `docker compose exec app php artisan migrate:fresh --seed`, then submit the homepage form.
  - TASK 08 should replace the temporary service number logic with the official `SN-YYYY-0001` yearly sequence generator.

### TASK 08 — Auto Service Number Generator
- Date/time: 2026-05-10 14:35:37 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected `ServiceNoteController`, `ServiceNote` model, sample service note seeder, and Composer autoload settings.
  - Confirmed TASK 08 was the next incomplete task.
  - Created `App\Services\ServiceNumberGenerator`.
  - Implemented official service number format: `SN-YYYY-0001`.
  - Generator defaults to current year and can accept an explicit year.
  - Generator reads existing yearly `service_no` values, includes soft-deleted records through `withTrashed()`, and increments the highest existing padded number.
  - Generator uses `lockForUpdate()` so the lookup is protected when called inside the existing database transaction.
  - Replaced the TASK 07 temporary `SN-TEMP-...` logic in `ServiceNoteController`.
  - Removed the temporary service number method from the controller.
- Files created:
  - `app/Services/ServiceNumberGenerator.php`
- Files changed:
  - `app/Http/Controllers/ServiceNoteController.php`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw -LiteralPath .\PROGRESS.md`
  - `Get-Content -Raw -LiteralPath .\TASKS.md`
  - `rg --files app routes resources database config | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNote.php`
  - `Get-Content -Raw -LiteralPath .\database\seeders\SampleServiceNoteSeeder.php`
  - `Get-Content -Raw -LiteralPath .\composer.json`
  - `Get-Content -Raw -LiteralPath .\app\Services\ServiceNumberGenerator.php`
  - `rg "SN-TEMP|temporaryServiceNo|ServiceNumberGenerator|SN-\{\$year\}|lockForUpdate|str_pad" app database routes resources`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - Confirmed `ServiceNumberGenerator` exists.
  - Confirmed generator uses `SN-{$year}-`, `str_pad(..., 4, '0', STR_PAD_LEFT)`, and `lockForUpdate()`.
  - Confirmed `ServiceNoteController` uses `ServiceNumberGenerator`.
  - Confirmed no `SN-TEMP` or `temporaryServiceNo` references remain.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Runtime generation/database save was not tested due to PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate with `docker compose exec app php artisan migrate:fresh --seed`, then submit a new form and confirm the next service number after the sample record is `SN-{current year}-0002`.
  - Next task should implement search old records on the homepage.

### TASK 09 — Search Old Records on Homepage
- Date/time: 2026-05-10 14:41:55 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected the current Laravel codebase, including routes, homepage view, service note controller, service note model, and service note migration.
  - Confirmed TASK 09 was the next incomplete task.
  - Added `ServiceNoteController@index` for the homepage.
  - Changed route `/` to use `ServiceNoteController@index`.
  - Added backend search over:
    - service number.
    - customer name, phone, and email.
    - device brand/model and serial number.
    - reported issue.
    - received date.
    - status.
    - technician name.
  - Added practical filters for status, device type, received date from, and received date to.
  - Added eager loading for customer and device records.
  - Added paginated search results with 10 records per page.
  - Replaced disabled search UI with a working GET search form on the homepage.
  - Added a full search result table with service number, received date, customer, phone, device type, brand/model, status, technician, total charge, and actions.
  - Added result action links for View, Edit, Print PDF, and Download PDF as planned URLs for later tasks.
  - Rebuilt frontend assets with Vite/Tailwind.
- Files created:
  - None.
- Files changed:
  - `app/Http/Controllers/ServiceNoteController.php`
  - `routes/web.php`
  - `resources/views/home.blade.php`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw PROGRESS.md`
  - `Get-Content -Raw TASKS.md`
  - `rg --files`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\home.blade.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNote.php`
  - `Get-Content -Raw -LiteralPath .\database\migrations\2026_05_10_000003_create_service_notes_table.php`
  - `rg -n "public function index|whereHas\('customer'|whereHas\('device'|paginate\(10\)|Search Results|received_from|Download PDF|service-notes/.+pdf|Route::get\('/', \[ServiceNoteController::class, 'index'\]" app routes resources`
  - `npm run build`
  - `rg -n "public function index|keyword|where\('service_no'|reported_issue|received_date|technician_name|whereHas\('customer'|whereHas\('device'|name', 'like'|phone', 'like'|email', 'like'|brand_model|serial_number|paginate\(10\)|Search Results|View</a>|Edit</a>|Print PDF|Download PDF" app routes resources`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed `/` route now uses `ServiceNoteController@index`.
  - Confirmed backend search covers required searchable fields through `ServiceNote`, `Customer`, and `Device`.
  - Confirmed search UI includes keyword, status, device type, and received date range filters.
  - Confirmed result table includes required columns and View/Edit/Print PDF/Download PDF actions.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Runtime homepage rendering and database search behavior were not tested due to PHP/Docker environment limitations.
- Notes:
  - The View/Edit/PDF action links intentionally point to planned URLs; their pages/routes are still future tasks.
  - Once Docker is running, validate with `docker compose exec app php artisan migrate:fresh --seed`, then search for `SN-`, `Ahmad`, `0123456789`, `Lenovo`, `TEST12345`, `Laptop tidak boleh ON`, `Received`, and a technician name after creating a record with one.
  - Next task should create the service note detail page.

### TASK 10 — View Service Note Detail Page
- Date/time: 2026-05-10 14:46:02 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected the current codebase, including routes, controller, layout, customer model, device model, and existing homepage result actions.
  - Confirmed TASK 10 was the next incomplete task.
  - Added `ServiceNoteController@show`.
  - Added GET route `/service-notes/{serviceNote}` named `service-notes.show`.
  - Updated homepage search result View action to use the named detail route.
  - Created `resources/views/service-notes/show.blade.php`.
  - Added detail sections for:
    - Maklumat Service.
    - Maklumat Pelanggan.
    - Maklumat Peranti.
    - Butiran Kerja.
    - Kos Baiki.
    - Waranti Servis.
    - Signature status.
  - Added action buttons for Edit, Print PDF, Download PDF, and New Form.
  - Kept Edit/PDF buttons as planned URLs for future tasks.
  - Hid device password by default on the detail page; the page shows only a hidden notice and does not render the password value.
  - Rebuilt frontend assets with Vite/Tailwind.
- Files created:
  - `resources/views/service-notes/show.blade.php`
- Files changed:
  - `app/Http/Controllers/ServiceNoteController.php`
  - `routes/web.php`
  - `resources/views/home.blade.php`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw PROGRESS.md`
  - `Get-Content -Raw TASKS.md`
  - `rg --files app routes resources database config | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\layouts\app.blade.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Customer.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Device.php`
  - `npm run build`
  - `rg -n "public function show|service-notes.show|Route::get\('/service-notes/\{serviceNote\}'|service-notes/show|Maklumat Pelanggan|Maklumat Peranti|Butiran Kerja|Kos Baiki|Waranti Servis|Disembunyikan secara default|Edit</a>|Print PDF|Download PDF|Back to search" app routes resources`
  - `rg -n "device_password" resources\views\service-notes\show.blade.php app\Http\Controllers\ServiceNoteController.php app\Models\Device.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\service-notes\show.blade.php`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed `service-notes.show` route exists.
  - Confirmed `ServiceNoteController@show` loads customer and device relations.
  - Confirmed detail Blade page exists and includes detail sections plus action buttons.
  - Confirmed detail Blade page does not contain a `device_password` value reference and shows `Disembunyikan secara default` instead.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Runtime detail page rendering was not tested due to PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate with `docker compose exec app php artisan migrate:fresh --seed`, then open `/service-notes/1`.
  - Next task should implement editing and updating service notes.

### TASK 11 — Edit and Update Service Note
- Date/time: 2026-05-10 14:50:59 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected the current codebase, including routes, controller, homepage form, service note detail page, and service note log model.
  - Confirmed TASK 11 was the next incomplete task.
  - Added `ServiceNoteController@edit`.
  - Added `ServiceNoteController@update`.
  - Added reusable service note validation rules for store/update.
  - Added reusable device type options in the controller.
  - Added GET route `/service-notes/{serviceNote}/edit` named `service-notes.edit`.
  - Added PUT route `/service-notes/{serviceNote}` named `service-notes.update`.
  - Updated existing Edit buttons to use the named edit route.
  - Created `resources/views/service-notes/edit.blade.php`.
  - Built the edit form with normal editable fields for service, customer, device, issue, diagnosis, repair action, parts, charges, and warranty.
  - Kept service number readonly on the edit page.
  - Recalculated `total_charge` in the backend from `service_charge + parts_charge`.
  - Added update flow for customer, device, and service note fields inside a database transaction.
  - Added `updated` log creation after successful update.
  - Kept device password sensitive by leaving the edit field blank; if blank on update, the existing password is preserved.
  - Added success flash display on the service note detail page after update.
  - Rebuilt frontend assets with Vite/Tailwind.
- Files created:
  - `resources/views/service-notes/edit.blade.php`
- Files changed:
  - `app/Http/Controllers/ServiceNoteController.php`
  - `routes/web.php`
  - `resources/views/home.blade.php`
  - `resources/views/service-notes/show.blade.php`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw PROGRESS.md`
  - `Get-Content -Raw TASKS.md`
  - `rg --files app routes resources database config | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\home.blade.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\service-notes\show.blade.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNoteLog.php`
  - `npm run build`
  - `rg -n "public function edit|public function update|private function serviceNoteRules|service-notes.edit|service-notes.update|Route::get\('/service-notes/\{serviceNote\}/edit'|Route::put\('/service-notes/\{serviceNote\}'|@method\('PUT'\)|Update Service Note|readonly|total_charge' => \$serviceCharge \+ \$partsCharge|'action' => 'updated'|service_no" app routes resources\views\service-notes resources\views\home.blade.php`
  - `rg -n "device_password" resources\views\service-notes\edit.blade.php app\Http\Controllers\ServiceNoteController.php`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed edit and update routes exist.
  - Confirmed edit page form submits with `PUT` to `service-notes.update`.
  - Confirmed service number field is readonly on the edit page.
  - Confirmed backend update recalculates `total_charge`.
  - Confirmed update creates an `updated` service note log.
  - Confirmed edit page does not prefill device password and the controller preserves the existing password when the field is left blank.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Runtime edit/update behavior was not tested due to PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate with `docker compose exec app php artisan migrate:fresh --seed`, then open `/service-notes/1/edit`, update charges/status, and confirm detail page plus logs update.
  - Next task should implement delete service note with confirmation.

### TASK 12 — Delete Service Note with Confirmation
- Date/time: 2026-05-10 14:53:47 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected the current codebase, including routes, service note controller, service note detail page, edit page, and `ServiceNote` soft delete support.
  - Confirmed TASK 12 was the next incomplete task.
  - Added `ServiceNoteController@destroy`.
  - Added DELETE route `/service-notes/{serviceNote}` named `service-notes.destroy`.
  - Added delete confirmation UI to the service note detail page.
  - Added irreversible-action warning text on the delete section.
  - Added service number confirmation input before delete.
  - Added browser confirmation before submitting the delete form.
  - Added validation that `delete_confirmation` must match the service note number.
  - Added `deleted` audit log creation before deleting.
  - Used existing `SoftDeletes` support on `ServiceNote` so the record is removed from active listings without hard deletion.
  - Rebuilt frontend assets with Vite/Tailwind.
- Files created:
  - None.
- Files changed:
  - `app/Http/Controllers/ServiceNoteController.php`
  - `routes/web.php`
  - `resources/views/service-notes/show.blade.php`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw PROGRESS.md`
  - `Get-Content -Raw TASKS.md`
  - `rg --files app routes resources database config | Sort-Object`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\service-notes\show.blade.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\service-notes\edit.blade.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\ServiceNote.php`
  - `npm run build`
  - `rg -n "public function destroy|service-notes.destroy|Route::delete|delete_confirmation|'action' => 'deleted'|soft deleted|->delete\(\)|Delete Service Note|@method\('DELETE'\)|irreversible|confirm\(" app routes resources\views\service-notes\show.blade.php`
  - `rg -n "SoftDeletes|softDeletes\(" app database`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed `service-notes.destroy` route exists.
  - Confirmed detail page contains DELETE form, confirmation input, irreversible warning text, and browser confirmation.
  - Confirmed controller validates confirmation text against `service_no`.
  - Confirmed controller creates `deleted` log before calling `$serviceNote->delete()`.
  - Confirmed `ServiceNote` uses `SoftDeletes` and migration includes `softDeletes()`.
  - `docker compose config --quiet` passed.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Runtime delete/soft-delete behavior was not tested due to PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate with `docker compose exec app php artisan migrate:fresh --seed`, open `/service-notes/1`, type the exact service number in the delete confirmation field, submit delete, and confirm the record no longer appears in active search results.
  - Next task should implement PDF template and PDF generation.

### TASK 13 — PDF Template and PDF Generation
- Date/time: 2026-05-10 14:58:19 +08:00
- Summary:
  - Re-read `PROGRESS.md` and `TASKS.md`.
  - Inspected the current codebase, including routes, service note controller, settings seeder/model, Dockerfile, and Composer configuration.
  - Confirmed TASK 13 was the next incomplete task.
  - Checked official package metadata from Packagist for `barryvdh/laravel-dompdf`; current package supports Laravel 13 through `illuminate/support ^13.0`.
  - Checked official Dompdf package requirements from Packagist; Dompdf requires PHP DOM and MBString extensions.
  - Added `barryvdh/laravel-dompdf` to `composer.json`.
  - Updated `Dockerfile` to install `libonig-dev`, `libxml2-dev`, and PHP extensions `dom` and `mbstring` for Dompdf compatibility.
  - Added `ServiceNoteController@pdf`.
  - Added GET route `/service-notes/{serviceNote}/pdf` named `service-notes.pdf`.
  - Created `resources/views/service-notes/pdf.blade.php`.
  - Built an A4 portrait service report PDF template based on the PDF reference structure:
    - company/header area.
    - service number box.
    - customer and device information sections.
    - reported issue.
    - initial diagnosis.
    - repair action taken.
    - parts and charges.
    - service warranty.
    - customer and technician signature lines.
  - Used company settings where available for company name, address, phone, office phone, support email, footer note, and default warranty note.
  - Excluded device password from the PDF template.
  - Rebuilt frontend assets with Vite/Tailwind.
- Sources checked:
  - Packagist `barryvdh/laravel-dompdf`: supports `illuminate/support ^9|^10|^11|^12|^13.0`.
  - Packagist `dompdf/dompdf`: requires PHP DOM and MBString extensions.
- Files created:
  - `resources/views/service-notes/pdf.blade.php`
- Files changed:
  - `composer.json`
  - `Dockerfile`
  - `app/Http/Controllers/ServiceNoteController.php`
  - `routes/web.php`
  - `public/build/manifest.json`
  - `public/build/assets/*`
  - `TASKS.md`
  - `PROGRESS.md`
- Commands run:
  - `Get-Content -Raw PROGRESS.md`
  - `Get-Content -Raw TASKS.md`
  - `rg --files app routes resources database config | Sort-Object`
  - `Get-Content -Raw composer.json`
  - Web check: Packagist `barryvdh/laravel-dompdf`
  - Web check: Packagist `dompdf/dompdf`
  - `Get-Content -Raw -LiteralPath .\Dockerfile`
  - `Get-Content -Raw -LiteralPath .\app\Http\Controllers\ServiceNoteController.php`
  - `Get-Content -Raw -LiteralPath .\routes\web.php`
  - `Get-Content -Raw -LiteralPath .\database\seeders\DefaultSettingsSeeder.php`
  - `Get-Content -Raw -LiteralPath .\app\Models\Setting.php`
  - `rg -n "barryvdh/laravel-dompdf|Barryvdh\\DomPDF\\Facade\\Pdf|public function pdf|service-notes.pdf|Route::get\('/service-notes/\{serviceNote\}/pdf'|loadView\('service-notes.pdf'|setPaper\('a4', 'portrait'\)|stream\(" composer.json app routes resources\views\service-notes\pdf.blade.php`
  - `rg -n "device_password|Password" resources\views\service-notes\pdf.blade.php`
  - `Get-Content -Raw -LiteralPath .\resources\views\service-notes\pdf.blade.php`
  - `Get-Content -Raw composer.json | ConvertFrom-Json | Out-Null; Write-Output 'composer.json valid JSON'`
  - `rg -n "barryvdh/laravel-dompdf|libonig-dev|libxml2-dev|docker-php-ext-install|dom|mbstring|public function pdf|service-notes.pdf|loadView\('service-notes.pdf'|setPaper\('a4', 'portrait'\)|device_password|Password" composer.json Dockerfile app routes resources\views\service-notes\pdf.blade.php`
  - `npm run build`
  - `composer --version`
  - `Select-String -Path .\resources\views\service-notes\pdf.blade.php -Pattern 'device_password','Password' -SimpleMatch`
  - `docker compose config --quiet`
  - `php -v`
  - `docker compose up -d --build`
  - `Get-Date -Format 'yyyy-MM-dd HH:mm:ss zzz'`
- Test result:
  - `composer.json` is valid JSON.
  - `npm run build` passed and generated updated Vite/Tailwind assets.
  - Confirmed `barryvdh/laravel-dompdf` is listed in `composer.json`.
  - Confirmed Dockerfile includes `dom` and `mbstring` PHP extension installation.
  - Confirmed `service-notes.pdf` route exists.
  - Confirmed `ServiceNoteController@pdf` loads the PDF view, sets A4 portrait paper, and streams a PDF response.
  - Confirmed the PDF Blade template exists and does not contain `device_password` or `Password`.
  - `docker compose config --quiet` passed.
  - `composer --version` failed because Composer is not installed locally.
  - `php -v` failed because PHP is not installed locally.
  - `docker compose up -d --build` failed because Docker Desktop Linux engine / Docker daemon is not active.
  - Composer install and runtime PDF generation were not tested due to Composer/PHP/Docker environment limitations.
- Notes:
  - Once Docker is running, validate with `docker compose up -d --build`, then `docker compose exec app composer install`, `docker compose exec app php artisan migrate:fresh --seed`, and open `/service-notes/1/pdf`.
  - Next task should implement print and download PDF buttons.

## Resume Instructions
To continue, tell Codex:
“Read PROGRESS.md and TASKS.md, inspect the codebase, then continue with the next incomplete task only.”
